# image-grabber（lychee-web-grabber）VPS 升级指南

本文档说明如何把 GitHub 仓库 `dgltsp-cpu/lychee-web-grabber` 的最新代码同步到 VPS 上的部署目录。

## 一、前提：VPS 能拉取私有仓库

Deploy Key 按仓库生效，grabber 使用独立的 `grabber_deploy` 钥匙：

1. 生成密钥（如果还没有）：

   ```bash
   ssh-keygen -t ed25519 -f ~/.ssh/grabber_deploy -N "" -C "grabber-deploy"
   cat ~/.ssh/grabber_deploy.pub
   ```

2. 把公钥加到 GitHub：`dgltsp-cpu/lychee-web-grabber` → Settings → Deploy keys → Add deploy key（不要勾选写权限）。

3. 在 `~/.ssh/config` 加一个别名（不占用 github.com 主配置）：

   ```text
   Host github.com-grabber
     HostName github.com
     User git
     IdentityFile ~/.ssh/grabber_deploy
     IdentitiesOnly yes
   ```

   保存后执行 `chmod 600 ~/.ssh/config`。

4. 把仓库 remote 指向别名，并验证：

   ```bash
   cd ~/lychee-web-grabber
   git remote set-url origin git@github.com-grabber:dgltsp-cpu/lychee-web-grabber.git
   ssh -T git@github.com-grabber
   # 期望：Hi dgltsp-cpu/lychee-web-grabber! You've successfully authenticated...
   ```

## 二、日常升级

```bash
cd ~/lychee-web-grabber
git pull --ff-only
docker compose up -d --build
```

`docker compose up -d --build` 会重新构建镜像，代码改动必须重建才生效。首次构建需要下载 Playwright Chromium，约 3~8 分钟。

## 三、验证

```bash
docker compose ps                      # image-grabber 应为 Up
curl -s http://127.0.0.1:8100 | head   # 返回页面 HTML 即成功
```

## 四、本地修改后的发布流程

```bash
cd lychee-web-grabber    # 本地仓库
git add -A
git commit -m "描述你的改动"
git push origin main
```

然后在 VPS 执行第二节的升级命令。

> 说明：`.env`（含 `LYCHEE_TOKEN` 等）已在 `.gitignore` 中，拉取不会覆盖。

## 五、常见问题

| 现象 | 处理 |
| --- | --- |
| `Permission denied (publickey)` | grabber 的 Deploy Key 未配置，按第一节检查；Deploy Key 按仓库生效，不能复用其他仓库的钥匙 |
| `network lychee_lychee declared as external, but could not be found` | Lychee 未启动，先启动 Lychee 再升级 grabber |
| 抓图页连不上 Lychee | 检查 `.env` 的 `LYCHEE_URL=http://lychee-api:8000` 与 `LYCHEE_TOKEN` |
| 构建很慢 | 首次构建需下载 Chromium，属正常现象；后续小改动会走 Docker 构建缓存 |
